const stringUpperCase = (word) => word.toUpperCase();

const firstLetter = (word) => word.substr(0, 1);

const concateString = (word1, word2) => word1.concat(word2);

module.exports = { 
  stringUpperCase, 
  firstLetter,
  concateString
};
