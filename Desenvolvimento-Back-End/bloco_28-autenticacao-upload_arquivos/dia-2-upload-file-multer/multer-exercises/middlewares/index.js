const error = require('./error');
const multerUpload = require('./multerUpload');
const multerMultiUpload = require('./multerMultiUpload');

module.exports = {
  error,
  multerUpload,
  multerMultiUpload,
};
